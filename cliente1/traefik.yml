entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"

providers:
  docker:
    exposedByDefault: false

tls:
  certificates:
    - certFile: "/certs/sangue.crt"
      keyFile: "/certs/sangue.key"

api:
  insecure: true
  dashboard: true
